---

import Breadcrumbs from '@/components/Breadcrumbs.astro'
import Link from '@/components/Link.astro'
import Layout from '@/layouts/Layout.astro'
import Container from '@/components/Container.astro'
import PaginationComponent from '@/components/ui/pagination'
import type { PaginateFunction } from 'astro'
// import { Button } from '@/components/ui/button'
// import { SITE } from '@/consts'
// import { Image } from 'astro:assets'
// import Authors from '@/components/Authors.astro'

// import BlogCard from '@/components/BlogCard.astro'


import cities from "@/data/cities.json"

export const prerender = true;

export async function getStaticPaths({
  paginate,
}: {
  paginate: PaginateFunction
}) {

  return paginate(
    cities,
    { pageSize: 90 },
  )
}

// type Props = {
//   id: number; 
//   name: string; 
//   slug: string; 
//   province_id: number;
// }

const { page } = Astro.props

const pageCities = page.data

---

<Layout
  title={` کافی نت دی در شهرهای ایران - کافی نت دی؛ کافی نتی روستایی با دسترسی جهانی`}
  description={``}
  image={'/static/daynetcafe-digital-banner.png'}
>
<Container class="flex grow flex-col gap-y-6">

  <section
    class="grid grid-cols-[minmax(0px,1fr)_min(768px,100%)_minmax(0px,1fr)] gap-y-6 *:px-4"
  >
    <Breadcrumbs
        items={[
            { href: '/location/cities', label: 'شهر', icon: 'lucide:archive' },
        ]}
        class="col-start-2"
        />

    <article
      class="prose prose-neutral col-start-2 max-w-none dark:prose-invert"
    >
        <h1>شهرها</h1>
        <p>شما عزیزان می توانید از خدمات کافی نت دی در شهرهای زیر بهره مند شوید. برای انجام کارهای کافی نتی خودتون می تونید روی ما حساب کنید. آنلاین یا حضوری، کافی نت دی در خدمت شماست. در کافی نت دی ما از پیشرفته ترین و به روز ترین نرم افزارها و روش های انجام پروژه ها برای ارائه خدمات استفاده میکنیم.</p>
        <ul class="grid grid-cols-2 md:grid-cols-3 gap-4 list-none pr-0">
            {pageCities.map(city => {
                return (
                    <li class="border h-auto max-w-full rounded-xl py-5 text-center"><Link href={`/location/cities/${city.slug}`}>{city.name}</Link></li>
                )
            })}
        </ul>
    </article>
</section>
<PaginationComponent
          currentPage={page.currentPage}
          totalPages={page.lastPage}
          baseUrl="/location/cities/"
          client:load
        />
</Container>
</Layout>
